import { Button } from "@pf-dev/ui";

import { env } from "@/config/env";

export function HomePage() {
  return (
    <div className="container py-10">
      <div className="max-w-2xl mx-auto text-center space-y-6">
        <h1 className="text-4xl font-bold tracking-tight">
          Welcome to {{ name }}
        </h1>
        <p className="text-lg text-muted-foreground">
          Built with React, TypeScript, Vite, Zustand, and Tailwind CSS
        </p>

        <div className="flex justify-center gap-4">
          <Button>Get Started</Button>
          <Button variant="outline">Learn More</Button>
        </div>

        {env.isDev && (
          <div className="mt-8 p-4 rounded-lg bg-muted text-sm text-left font-mono">
            <p className="text-muted-foreground mb-2">Environment Info:</p>
            <ul className="space-y-1">
              <li>App: {env.appName}</li>
              <li>Env: {env.appEnv}</li>
              <li>API: {env.apiUrl}</li>
            </ul>
          </div>
        )}
      </div>
    </div>
  );
}
